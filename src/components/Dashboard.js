import React from 'react';

const Dashboard = (props) => { //props.accessToken to make requests to server
    return (
        <div>
            Hello, this is your access token: 
        </div>
    )
}

export default Dashboard;